# ===============================
# PASSENGER + FLASK-SOCKETIO CONFIGURATION
# ===============================
# Routes /socket.io and all other requests to Flask app
# This ensures Socket.IO requests reach Flask-SocketIO instead of returning 404

PassengerEnabled on
PassengerAppRoot /home/impromptuindian/backend
PassengerAppType wsgi
PassengerStartupFile passenger_wsgi.py

RewriteEngine On

# ✅ CRITICAL: Allow Socket.IO - explicit rule first
RewriteCond %{REQUEST_URI} ^/socket.io
RewriteRule ^(.*)$ passenger_wsgi.py/$1 [QSA,L]

# Route all other requests to Flask
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ passenger_wsgi.py/$1 [QSA,L]
